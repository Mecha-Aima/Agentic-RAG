chunking:
  chunk_size: 512
  chunk_overlap: 50
  separators: ["\n\n", "\n", " ", ""]

embedding:
  batch_size: 100

graph:
  concurrency: 1
  enforce_schema: true

vector_db:
  collection_name: "rag_collection"
  distance_metric: "Cosine"
