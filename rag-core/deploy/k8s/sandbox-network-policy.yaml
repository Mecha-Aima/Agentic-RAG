# Optional: requires a CNI that enforces NetworkPolicy (not default K3s flannel)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sandbox-deny-egress
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: sandbox-service
  policyTypes:
    - Egress
  egress: []
